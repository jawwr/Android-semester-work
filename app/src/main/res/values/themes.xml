<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SemesterWork" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SemesterWork" parent="Base.Theme.SemesterWork">
        <item name="android:windowBackground">@drawable/background_yellow</item>
        <item name="android:gravity">center</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="AppTheme.Launcher" parent="Base.Theme.SemesterWork">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="White_background">
        <item name="android:windowBackground">@drawable/background_white</item>
        <item name="android:gravity">center</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="cartDishPhotoImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">7dp</item>
    </style>

    <style name="ButtonNoShadow" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/counter_button_shape</item>
        <item name="android:stateListAnimator">@null</item>
    </style>
</resources>